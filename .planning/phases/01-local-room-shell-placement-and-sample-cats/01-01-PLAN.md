---
phase: 01-local-room-shell-placement-and-sample-cats
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/room/room_constants.gd
  - scripts/room/room_shell.gd
  - scripts/room/room_occlusion_controller.gd
  - scenes/room/room_shell.tscn
  - scenes/main.tscn
  - scripts/player.gd
autonomous: true
requirements:
  - CTRL-01
  - SHELL-01
  - SHELL-02
  - PERF-01
  - SCOPE-01
user_setup: []
must_haves:
  truths:
    - "Phase 1 starts by replacing the open test platform with a real enclosed room shell."
    - "Wall and ceiling occlusion work with the current Godot camera modes instead of click-to-move assumptions."
    - "The player stays inside room-aware bounds without regressing the existing direct movement controller."
  artifacts:
    - path: "scripts/room/room_shell.gd"
      provides: "Room shell controller with hideable floor, wall, and ceiling groups"
      min_lines: 60
      contains: "class_name RoomShell"
    - path: "scripts/room/room_occlusion_controller.gd"
      provides: "Camera-driven wall and ceiling visibility controller"
      min_lines: 40
      contains: "class_name RoomOcclusionController"
    - path: "scenes/room/room_shell.tscn"
      provides: "Reusable enclosed room shell scene"
      min_lines: 20
      contains: "[node name=\"RoomShell\""
  key_links:
    - from: "scripts/room/room_shell.gd"
      to: "Z:\\FAHHHH\\src\\components\\room-view\\RoomShell.tsx"
      via: "Room shell surface layering and hideable wall structure"
      pattern: "RoomShell"
    - from: "scripts/room/room_occlusion_controller.gd"
      to: "Z:\\FAHHHH\\src\\components\\room-view\\WallOcclusionController.tsx"
      via: "Angle- and height-based wall/ceiling occlusion logic"
      pattern: "wall_front"
---

<objective>
Build the enclosed room shell and occlusion foundation that the rest of the local builder will sit on.

Purpose: Replace the current open prototype ground with a room the player can actually walk inside, while preserving the existing direct-movement and camera controls.
Output: A reusable room shell scene, a camera-driven occlusion controller, and room-aware player bounds integrated into the live world.
</objective>

<execution_context>
@C:\Users\user\.codex\get-shit-done\workflows\execute-plan.md
@C:\Users\user\.codex\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-local-room-shell-placement-and-sample-cats/01-CONTEXT.md
@.planning/phases/01-local-room-shell-placement-and-sample-cats/01-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the reusable enclosed room shell</name>
  <files>scripts/room/room_constants.gd, scripts/room/room_shell.gd, scenes/room/room_shell.tscn</files>
  <read_first>scenes/main.tscn, scripts/player.gd, Z:\FAHHHH\src\components\room-view\RoomShell.tsx, Z:\FAHHHH\docs\CURRENT_SYSTEMS.md</read_first>
  <action>Create a reusable room shell subsystem with `class_name RoomShell` in `scripts/room/room_shell.gd`, a supporting constants file in `scripts/room/room_constants.gd`, and `scenes/room/room_shell.tscn`. The shell must provide a floor, four walls, and a roof/ceiling using browser-safe Godot primitives or simple meshes, with distinct node groups or child nodes for `wall_back`, `wall_left`, `wall_front`, `wall_right`, and `ceiling` so later logic can show, hide, or fade them independently. Expose room dimensions and any simple materials as exported properties. Do not introduce backend, multiplayer, or click-to-move behavior.</action>
  <verify>rg "class_name RoomShell|wall_back|wall_left|wall_front|wall_right|ceiling" scripts/room/room_shell.gd scenes/room/room_shell.tscn</verify>
  <acceptance_criteria>
    - scripts/room/room_shell.gd contains "class_name RoomShell"
    - scenes/room/room_shell.tscn contains "wall_back"
    - scenes/room/room_shell.tscn contains "wall_front"
    - scenes/room/room_shell.tscn contains "ceiling"
  </acceptance_criteria>
  <done>The repo has a reusable enclosed room shell that can become the authoritative room geometry for placement and cat systems.</done>
</task>

<task type="auto">
  <name>Task 2: Implement camera-driven wall and ceiling occlusion</name>
  <files>scripts/room/room_occlusion_controller.gd, scripts/room/room_shell.gd</files>
  <read_first>scripts/player.gd, Z:\FAHHHH\src\components\room-view\WallOcclusionController.tsx, .planning/phases/01-local-room-shell-placement-and-sample-cats/01-RESEARCH.md</read_first>
  <action>Create `scripts/room/room_occlusion_controller.gd` with `class_name RoomOcclusionController`. The controller must observe the active player camera, derive the camera angle and vertical relationship to the room center, and toggle or fade `wall_front`, `wall_back`, `wall_left`, `wall_right`, and `ceiling` visibility so the interior stays readable. It must work with the existing freecam, third-person, and first-person camera modes, and it must not assume click-to-move or orbit-only controls.</action>
  <verify>rg "class_name RoomOcclusionController|wall_front|wall_back|wall_left|wall_right|ceiling" scripts/room/room_occlusion_controller.gd</verify>
  <acceptance_criteria>
    - scripts/room/room_occlusion_controller.gd contains "class_name RoomOcclusionController"
    - scripts/room/room_occlusion_controller.gd contains "wall_front"
    - scripts/room/room_occlusion_controller.gd contains "ceiling"
    - scripts/room/room_shell.gd contains "set_surface_visible"
  </acceptance_criteria>
  <done>The room shell can react to the current camera position and keep the room interior readable without deleting the shell structure.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate the room shell into the live world and replace hard-coded platform bounds</name>
  <files>scenes/main.tscn, scripts/player.gd</files>
  <read_first>scenes/main.tscn, scripts/player.gd, .planning/codebase/CONCERNS.md</read_first>
  <action>Integrate the new room shell into `scenes/main.tscn`, replacing or superseding the current open-platform world layout. Update `scripts/player.gd` so the player no longer depends on `PLATFORM_HALF_SIZE` and `_clamp_player_to_platform()`. Replace that behavior with exported or injected room-aware bounds and a method named `_clamp_player_to_room_bounds()`. Read the current `scenes/main.tscn` carefully and preserve unrelated local edits while wiring in the room shell, occlusion controller, and existing player node. Keep the current direct movement, camera switching, and skin UI intact.</action>
  <verify>rg "RoomShell|RoomOcclusionController|_clamp_player_to_room_bounds|room_bounds_half_extents" scenes/main.tscn scripts/player.gd</verify>
  <acceptance_criteria>
    - scenes/main.tscn contains "RoomShell"
    - scenes/main.tscn contains "RoomOcclusionController"
    - scripts/player.gd contains "_clamp_player_to_room_bounds"
    - scripts/player.gd does not contain "func _clamp_player_to_platform"
  </acceptance_criteria>
  <done>The live world now has a real room shell, the player remains inside it, and current movement/camera behavior still works.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] rg "class_name RoomShell" scripts/room/room_shell.gd
- [ ] rg "class_name RoomOcclusionController" scripts/room/room_occlusion_controller.gd
- [ ] rg "RoomShell|RoomOcclusionController" scenes/main.tscn
- [ ] rg "_clamp_player_to_room_bounds" scripts/player.gd
- [ ] Manual smoke check confirms the player can still walk and switch camera modes inside the new room shell
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The room shell is enclosed, readable, and ready for placement systems
- The current direct movement/camera controller remains the control baseline
</success_criteria>

<output>
After completion, create `.planning/phases/01-local-room-shell-placement-and-sample-cats/01-01-SUMMARY.md`
</output>
