---
phase: 01-local-room-shell-placement-and-sample-cats
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - scripts/build/build_item_registry.gd
  - scripts/build/placement_types.gd
  - scripts/build/placement_resolver.gd
  - scripts/build/build_mode_controller.gd
  - scripts/ui/build_toolbar.gd
  - scenes/ui/build_toolbar.tscn
  - scenes/main.tscn
  - scripts/player.gd
autonomous: true
requirements:
  - CTRL-01
  - PLAC-01
  - PLAC-02
  - PLAC-03
  - PLAC-04
  - PLAC-05
  - PERF-01
  - SCOPE-01
user_setup: []
must_haves:
  truths:
    - "Placement rules are explicit per family: floor, wall, ceiling, and anchored surface decor."
    - "The build flow stays local-only and works with the current movement/camera baseline."
    - "Surface decor preserves its host anchor instead of becoming free-floating room geometry."
  artifacts:
    - path: "scripts/build/build_item_registry.gd"
      provides: "Local sample build catalog with per-family placement metadata"
      min_lines: 50
      contains: "class_name BuildItemRegistry"
    - path: "scripts/build/placement_resolver.gd"
      provides: "Grid, wall, ceiling, and surface placement math"
      min_lines: 80
      contains: "class_name PlacementResolver"
    - path: "scripts/build/build_mode_controller.gd"
      provides: "Runtime build interaction orchestration"
      min_lines: 60
      contains: "class_name BuildModeController"
  key_links:
    - from: "scripts/build/placement_resolver.gd"
      to: "Z:\\FAHHHH\\src\\components\\room-view\\placementResolvers.ts"
      via: "Placement family resolution and drag/raycast rules"
      pattern: "resolveFloorPlacement"
    - from: "scripts/build/placement_resolver.gd"
      to: "Z:\\FAHHHH\\src\\lib\\surfaceDecor.ts"
      via: "Anchored surface support-host and local-offset rules"
      pattern: "anchorFurnitureId"
---

<objective>
Build the grid placement systems for floor, wall, ceiling, and anchored surface decor.

Purpose: Deliver the local room-builder interaction loop without dragging backend, inventory, or click-to-move systems into scope.
Output: A local sample build catalog, family-specific placement resolvers, and a minimal build mode UI/controller integrated into the room scene.
</objective>

<execution_context>
@C:\Users\user\.codex\get-shit-done\workflows\execute-plan.md
@C:\Users\user\.codex\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-local-room-shell-placement-and-sample-cats/01-CONTEXT.md
@.planning/phases/01-local-room-shell-placement-and-sample-cats/01-RESEARCH.md
@.planning/phases/01-local-room-shell-placement-and-sample-cats/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the local sample build catalog and placement data model</name>
  <files>scripts/build/build_item_registry.gd, scripts/build/placement_types.gd</files>
  <read_first>Z:\FAHHHH\docs\CURRENT_SYSTEMS.md, Z:\FAHHHH\src\components\room-view\placementResolvers.ts, Z:\FAHHHH\src\lib\surfaceDecor.ts</read_first>
  <action>Create `scripts/build/build_item_registry.gd` with `class_name BuildItemRegistry` and `scripts/build/placement_types.gd` for shared placement types/constants. Define a local-only sample build catalog with at least one item per placement family: floor, wall, ceiling, and surface decor. Each entry must declare the placement family, footprint, preview metadata, and any support-host or wall-target rules needed by the resolver. Use source-compatible surface names where relevant: `floor`, `wall_back`, `wall_left`, `wall_front`, `wall_right`, `ceiling`, and `surface`.</action>
  <verify>rg "class_name BuildItemRegistry|floor|wall_back|wall_left|wall_front|wall_right|ceiling|surface" scripts/build/build_item_registry.gd scripts/build/placement_types.gd</verify>
  <acceptance_criteria>
    - scripts/build/build_item_registry.gd contains "class_name BuildItemRegistry"
    - scripts/build/build_item_registry.gd contains "\"floor\""
    - scripts/build/build_item_registry.gd contains "\"ceiling\""
    - scripts/build/build_item_registry.gd contains "\"surface\""
  </acceptance_criteria>
  <done>The repo has a concrete local build catalog and a shared vocabulary for the four placement families.</done>
</task>

<task type="auto">
  <name>Task 2: Implement family-specific placement resolution and validity rules</name>
  <files>scripts/build/placement_resolver.gd, scripts/build/build_item_registry.gd</files>
  <read_first>Z:\FAHHHH\src\components\room-view\placementResolvers.ts, Z:\FAHHHH\src\lib\surfaceDecor.ts, scenes/room/room_shell.tscn</read_first>
  <action>Create `scripts/build/placement_resolver.gd` with `class_name PlacementResolver`. Implement floor snapping inside room bounds, wall placement across all four walls, ceiling placement against valid overhead cells, and surface-decor placement anchored to valid support hosts with persistent `anchor_item_id` and local offset data. Reject invalid placements instead of silently dropping them. Keep the logic local-only; do not add owned-vs-placed inventory, persistence, or networking beyond the minimum placement state needed for this phase.</action>
  <verify>rg "class_name PlacementResolver|anchor_item_id|wall_back|wall_left|wall_front|wall_right|ceiling|surface" scripts/build/placement_resolver.gd</verify>
  <acceptance_criteria>
    - scripts/build/placement_resolver.gd contains "class_name PlacementResolver"
    - scripts/build/placement_resolver.gd contains "anchor_item_id"
    - scripts/build/placement_resolver.gd contains "wall_front"
    - scripts/build/placement_resolver.gd contains "ceiling"
  </acceptance_criteria>
  <done>The repo has one placement resolver that understands all four placement families and anchored surface decor.</done>
</task>

<task type="auto">
  <name>Task 3: Wire a minimal build mode into the runtime without regressing movement</name>
  <files>scripts/build/build_mode_controller.gd, scripts/ui/build_toolbar.gd, scenes/ui/build_toolbar.tscn, scenes/main.tscn, scripts/player.gd</files>
  <read_first>scripts/player.gd, scripts/skin_picker.gd, scenes/main.tscn, .planning/phases/01-local-room-shell-placement-and-sample-cats/01-RESEARCH.md</read_first>
  <action>Create `scripts/build/build_mode_controller.gd` with `class_name BuildModeController` and a small UI layer in `scripts/ui/build_toolbar.gd` plus `scenes/ui/build_toolbar.tscn`. Add a minimal build loop that lets the player toggle build mode with `B`, rotate previews with `R`, place with a primary click, and cancel with `Esc` or a secondary click while keeping the current camera and direct movement baseline intact. The build UI must allow switching between the sample floor, wall, ceiling, and surface items. If `skin_picker.gd` needs to cooperate with the build UI, preserve its existing skin and camera controls.</action>
  <verify>rg "class_name BuildModeController|KEY_B|KEY_R|build_mode" scripts/build/build_mode_controller.gd scripts/player.gd scripts/ui/build_toolbar.gd</verify>
  <acceptance_criteria>
    - scripts/build/build_mode_controller.gd contains "class_name BuildModeController"
    - scripts/build/build_mode_controller.gd contains "KEY_B"
    - scripts/build/build_mode_controller.gd contains "KEY_R"
    - scripts/ui/build_toolbar.gd contains "class_name BuildToolbar"
  </acceptance_criteria>
  <done>The room sandbox now supports a minimal, usable local build mode without dragging in click-to-move or backend scope.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] rg "class_name BuildItemRegistry" scripts/build/build_item_registry.gd
- [ ] rg "class_name PlacementResolver" scripts/build/placement_resolver.gd
- [ ] rg "class_name BuildModeController" scripts/build/build_mode_controller.gd
- [ ] Manual smoke check confirms floor, wall, ceiling, and surface placement all work and invalid targets are rejected
- [ ] Manual smoke check confirms direct movement and current camera modes still work while build mode exists
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The room supports floor, wall, ceiling, and anchored surface placement
- The build loop stays local-only and preserves current movement/camera behavior
</success_criteria>

<output>
After completion, create `.planning/phases/01-local-room-shell-placement-and-sample-cats/01-02-SUMMARY.md`
</output>
