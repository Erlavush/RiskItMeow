---
phase: 02-room-view-camera-and-visual-fidelity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/camera/room_view_camera_controller.gd
  - scenes/main.tscn
  - scripts/player.gd
  - scripts/build/build_mode_controller.gd
  - scripts/room/room_occlusion_controller.gd
  - scripts/skin_picker.gd
autonomous: true
requirements:
  - CTRL-01
  - CAM-01
  - CAM-02
  - PERF-01
  - SCOPE-01
user_setup: []
must_haves:
  truths:
    - "The runtime uses one stable room-view orbit camera aimed at the room center."
    - "The old freecam / first-person / third-person switching path is no longer the active room camera baseline."
    - "Direct movement remains local-only and click-to-move stays out of scope."
  artifacts:
    - path: "scripts/camera/room_view_camera_controller.gd"
      provides: "Single room-view orbit camera controller with smooth zoom and fixed target"
      min_lines: 80
      contains: "class_name RoomViewCameraController"
  key_links:
    - from: "scripts/camera/room_view_camera_controller.gd"
      to: "Z:\\FAHHHH\\src\\components\\room-view\\useRoomViewCamera.ts"
      via: "Single-camera orbit behavior, smooth zoom, and reset framing"
      pattern: "zoomTargetDistanceRef"
    - from: "scripts/room/room_occlusion_controller.gd"
      to: "Z:\\FAHHHH\\src\\components\\room-view\\WallOcclusionController.tsx"
      via: "Occlusion tied to camera angle around a fixed room target"
      pattern: "ROOM_CAMERA_TARGET"
---

<objective>
Replace the current shaky multi-camera prototype with one stable room-view orbit camera.

Purpose: Make the room readable and stable before deeper visual work lands.
Output: A single room camera controller, updated player/build/occlusion hooks, and removal of the current camera-switching baseline.
</objective>

<execution_context>
@C:\Users\user\.codex\get-shit-done\workflows\execute-plan.md
@C:\Users\user\.codex\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-room-view-camera-and-visual-fidelity/02-CONTEXT.md
@.planning/phases/02-room-view-camera-and-visual-fidelity/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the room-view orbit camera controller</name>
  <files>scripts/camera/room_view_camera_controller.gd, scenes/main.tscn</files>
  <read_first>Z:\FAHHHH\src\components\room-view\useRoomViewCamera.ts, Z:\FAHHHH\src\lib\sceneTargets.ts, scenes/main.tscn</read_first>
  <action>Create `scripts/camera/room_view_camera_controller.gd` with `class_name RoomViewCameraController` and integrate it into `scenes/main.tscn`. The controller should own one `Camera3D`, orbit around a fixed room target near the center of the room, support smooth wheel zoom, disable panning, and expose the active room camera to downstream systems.</action>
  <verify>rg "class_name RoomViewCameraController|target_position|zoom" scripts/camera/room_view_camera_controller.gd</verify>
  <acceptance_criteria>
    - scripts/camera/room_view_camera_controller.gd contains "class_name RoomViewCameraController"
    - scripts/camera/room_view_camera_controller.gd contains "target_position"
    - scenes/main.tscn contains "RoomViewCameraController"
  </acceptance_criteria>
  <done>The scene has one explicit room camera controller instead of relying on the player rig for the main view.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire player, build mode, and occlusion around the new camera baseline</name>
  <files>scripts/player.gd, scripts/build/build_mode_controller.gd, scripts/room/room_occlusion_controller.gd</files>
  <read_first>scripts/player.gd, scripts/build/build_mode_controller.gd, scripts/room/room_occlusion_controller.gd</read_first>
  <action>Update player-facing hooks so movement still works but camera ownership moves to the new room-view camera controller. Build preview raycasts and occlusion should read from the room camera, not from the player's current camera stack. Remove any hard dependency on freecam / first-person / third-person switching as the active room baseline.</action>
  <verify>rg "get_active_camera|RoomViewCameraController|camera" scripts/player.gd scripts/build/build_mode_controller.gd scripts/room/room_occlusion_controller.gd</verify>
  <acceptance_criteria>
    - scripts/player.gd still contains "get_active_camera"
    - scripts/build/build_mode_controller.gd still resolves preview from an active camera hook
    - scripts/room/room_occlusion_controller.gd still updates visibility from the active room camera
  </acceptance_criteria>
  <done>Movement, build mode, and occlusion are all compatible with the one-camera room view.</done>
</task>

<task type="auto">
  <name>Task 3: Remove legacy room-camera switching from the active UI flow</name>
  <files>scripts/player.gd, scripts/skin_picker.gd</files>
  <read_first>scripts/player.gd, scripts/skin_picker.gd</read_first>
  <action>Simplify the player/UI camera controls so the room no longer presents freecam, first-person, and third-person as active gameplay modes. If the old code is kept temporarily for safety, it must be sidelined from the current runtime path and no longer be the default room-view behavior.</action>
  <verify>rg "F5|cycle_camera_mode|get_camera_mode_name" scripts/player.gd scripts/skin_picker.gd</verify>
  <acceptance_criteria>
    - The active room UI no longer depends on `F5` camera cycling
    - scripts/skin_picker.gd no longer advertises the old multi-camera room flow as the main camera model
  </acceptance_criteria>
  <done>The runtime presents one clear room-view camera model instead of a prototype stack of camera modes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] rg "class_name RoomViewCameraController" scripts/camera/room_view_camera_controller.gd
- [ ] rg "RoomViewCameraController" scenes/main.tscn
- [ ] Manual smoke check confirms the new orbit camera is stable and build preview remains usable
- [ ] Manual smoke check confirms no click-to-move was introduced
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The current shaky camera stack is replaced by a stable room-view orbit camera
- Direct movement remains in place and click-to-move remains absent
</success_criteria>

<output>
After completion, create `.planning/phases/02-room-view-camera-and-visual-fidelity/02-01-SUMMARY.md`
</output>
